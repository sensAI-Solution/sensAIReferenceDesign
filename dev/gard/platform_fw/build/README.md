# Build folder

This folder contains the main makefile which is invoked for compiling the code.
The artifacts are to be routed to an output folder in build/ so that
a rebuild is just a call to remove the output folder before calling make again.

